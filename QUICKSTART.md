# 🚀 Быстрый старт FuncXauusd v1.1

## Установка и первый запуск за 5 минут

### Шаг 1: Клонирование и установка (1 мин)

```bash
# Клонирование репозитория
git clone <repository-url>
cd FuncXauusd

# Создание виртуального окружения
python -m venv venv

# Активация (Linux/Mac)
source venv/bin/activate

# Активация (Windows)
venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

---

### Шаг 2: Подготовка данных (2 мин)

#### A. Экспорт данных из MetaTrader 5

1. Откройте MT5
2. Выберите инструмент XAUUSD
3. Перейдите в `View → Data Window`
4. Кликните правой кнопкой → `Export to CSV`
5. Сохраните как `XAUUSD_H1.csv` в `data/raw/`

Формат должен быть:
```csv
Date;Open;High;Low;Close;Volume
2004.06.11 07:00;384.00;384.30;383.30;383.80;44
2004.06.11 08:00;383.80;384.30;383.10;383.10;41
...
```

#### B. Опционально: Мультифреймовые данные

Для использования мультифреймовых признаков, экспортируйте также:
- `XAUUSD_D1.csv` (дневные)
- `XAUUSD_W1.csv` (недельные)
- `XAUUSD_MN.csv` (месячные)

---

### Шаг 3: Первое обучение (2 мин)

#### Вариант A: Базовое обучение (оригинальный функционал)

```bash
python scripts/train_cluster_model.py
```

Это запустит:
- Автопоиск оптимальной конфигурации (40 попыток)
- Целевая точность: 75%+
- Целевой R²: 0.95+

#### Вариант B: Расширенное обучение (новые возможности v1.1)

```bash
# С Walk-Forward валидацией
python scripts/train_with_walk_forward.py --enable-walk-forward

# С Walk-Forward + мультифрейм
python scripts/train_with_walk_forward.py \
    --enable-walk-forward \
    --enable-multiframe

# Полная конфигурация (рекомендуется)
python scripts/train_with_walk_forward.py \
    --enable-walk-forward \
    --enable-multiframe \
    --optimize-atr
```

**⚠️ Внимание:** Полная конфигурация займет больше времени (30-60 мин в зависимости от ПК).

---

### Шаг 4: Результаты

После успешного обучения вы увидите:

```
✅ ОБУЧЕНИЕ ЗАВЕРШЕНО УСПЕШНО!

📁 Результаты:
  • ONNX модели: ./models/exported/
  • Degradation monitor: ./logs/degradation_monitor_initial.json

💡 Следующие шаги:
  1. Скопировать ONNX в MT5/Experts/Files/
  2. Скопировать .mqh в MT5/Include/
  3. Запустить на демо-счете минимум 6 месяцев
  4. Мониторить degradation monitor
```

Файлы в `models/exported/`:
```
catmodel XAUUSD_H1 0.onnx           # Главная модель
catmodel_m XAUUSD_H1 0.onnx         # Мета-модель
XAUUSD_H1 ONNX include 0.mqh       # Include файл для MT5
```

---

## Интеграция с MetaTrader 5

### Шаг 1: Копирование файлов

```
MetaTrader 5/
└── MQL5/
    ├── Experts/
    │   └── Files/
    │       ├── catmodel XAUUSD_H1 0.onnx      ← Скопировать
    │       └── catmodel_m XAUUSD_H1 0.onnx    ← Скопировать
    │
    ├── Include/
    │   └── XAUUSD_H1 ONNX include 0.mqh       ← Скопировать
    │
    └── Experts/
        └── OneDirectionBot.mq5                 ← Уже есть
```

### Шаг 2: Обновление советника

Откройте `OneDirectionBot.mq5` в MetaEditor и обновите строку include:

```cpp
// БЫЛО:
#include <XAUUSD_H1 ONNX include 0.mqh>

// Если у вас другой номер модели:
#include <XAUUSD_H1 ONNX include N.mqh>  // где N - номер вашей модели
```

### Шаг 3: Компиляция

1. Нажмите `F7` в MetaEditor
2. Проверьте отсутствие ошибок
3. Если есть ошибки про размеры массивов - см. раздел "Troubleshooting"

### Шаг 4: Запуск на демо

1. Откройте график XAUUSD H1
2. Перетащите `OneDirectionBot` на график
3. Настройте параметры:
   ```
   direction = true            // true = Buy, false = Sell
   main_threshold = 0.5        // Порог основной модели
   meta_threshold = 0.5        // Порог мета-модели
   stoploss = 2000             // Stop-Loss в пунктах
   takeprofit = 500            // Take-Profit в пунктах
   ```
4. Нажмите "OK"

---

## Мониторинг

### Проверка работы советника

В логах MT5 (вкладка "Experts") должны быть:
```
✓ ONNX models loaded successfully!
  Main features: 10
  Meta features: 1
```

### Мониторинг деградации (важно!)

```bash
# Запуск скрипта мониторинга (TODO - будет в v1.2)
python scripts/monitor_live_trading.py
```

Пока что проверяйте вручную:
- Просадка < 120% от исторической
- Нет серий из 10+ убыточных сделок
- Средняя прибыль не упала ниже 50%

---

## Troubleshooting

### Ошибка: "Cannot set input shape for main model"

**Причина:** Несоответствие размеров массивов в MQL коде.

**Решение:**
В `OneDirectionBot.mq5` найдите:
```cpp
int total_main_features = ArraySize(PeriodsXAUUSD_H1_0);   // ПРАВИЛЬНО
int total_meta_features = ArraySize(Periods_mXAUUSD_H1_0); // ПРАВИЛЬНО
```

Убедитесь, что массивы `f[]` и `f_m[]` создаются правильно:
```cpp
double f[];
double f_m[];
ArrayResize(f, total_main_features);  // НЕ ArraySize(features)!
ArrayResize(f_m, total_meta_features); // НЕ ArraySize(features_m)!
```

### Ошибка: "File not found"

**Проверьте:**
1. Файлы ONNX в правильной директории: `MT5/MQL5/Experts/Files/`
2. Имена файлов точно совпадают (включая пробелы!)
3. Include файл в: `MT5/MQL5/Include/`

### Walk-Forward валидация не проходит

**Возможные причины:**
1. Недостаточно данных (минимум 1000+ баров)
2. Слишком агрессивные критерии в `WalkForwardConfig`
3. Модель переобучается

**Решение:**
Смягчите критерии:
```python
config = WalkForwardConfig(
    min_ppt=0.0,           # Было: 5.0
    max_drawdown=0.10,     # Было: 0.05
    min_sharpe=0.3,        # Было: 0.5
    max_retries=5          # Было: 3
)
```

### CSV не загружается

**Проверьте формат:**
```python
# Должен быть разделитель ';'
Date;Open;High;Low;Close;Volume

# НЕ запятая:
Date,Open,High,Low,Close,Volume
```

Если формат другой, используйте `pandas`:
```python
df = pd.read_csv('your_file.csv', sep=',')  # Укажите правильный разделитель
df.to_csv('XAUUSD_H1.csv', sep=';', index=False)
```

---

## Полезные команды

### Проверка структуры проекта
```bash
tree -L 3 -I '__pycache__|*.pyc|venv'
```

### Запуск тестов
```bash
pytest tests/test_new_modules.py -v
```

### Проверка конфигурации
```bash
python -c "import yaml; print(yaml.safe_load(open('config/training_config.yaml')))"
```

### Очистка кэша
```bash
find . -type d -name "__pycache__" -exec rm -r {} +
find . -type f -name "*.pyc" -delete
```

---

## Следующие шаги

1. **Изучите документацию:**
   - [Implementation Plan](docs/implementation_plan.md) - детальное описание методологии
   - [Project Structure](PROJECT_STRUCTURE.md) - архитектура проекта

2. **Экспериментируйте:**
   - Попробуйте разные `markup` значения
   - Оптимизируйте ATR множители
   - Протестируйте на разных инструментах

3. **Мониторьте:**
   - Обязательно используйте демо-счет минимум 6 месяцев
   - Следите за degradation monitor
   - Логируйте все сделки

4. **Улучшайте:**
   - Добавьте новые признаки
   - Реализуйте автоматическую перетренировку
   - Интегрируйте Telegram уведомления

---

## Получение помощи

1. **Документация:** Читайте `README.md` и `implementation_plan.md`
2. **Тесты:** Запускайте `pytest` для проверки функциональности
3. **Логи:** Проверяйте `logs/` директорию
4. **GitHub Issues:** Создавайте issues с детальным описанием проблемы

---

**🎉 Готово! Вы запустили FuncXauusd v1.1**

Следующий шаг: Запустите обучение и протестируйте на демо-счете.

**⚠️ Важно:** Никогда не торгуйте реальными деньгами без 6+ месяцев успешного форвард-теста на демо-счете!