# FuncXauusd - Production ML Trading System

> Кластерная торговая система для XAUUSD с автоматическим поиском оптимальных конфигураций

## 📋 Описание

Production-ready реализация торговой ML-системы на основе CatBoost с:
- **Двухуровневой архитектурой**: Main model (торговые сигналы) + Meta model (фильтрация кластеров)
- **Автоматическим поиском гиперпараметров**: 40 итераций с целевой точностью 75%+
- **ONNX экспортом** для интеграции с MetaTrader 5
- **Мультифреймовой поддержкой**: от 1m до 1Month

## 🏗️ Архитектура проекта

```
FuncXauusd/
├── data/                    # Исторические данные (не в git)
│   ├── raw/                # Исходные CSV файлы
│   └── processed/          # Обработанные данные
├── models/                  # Обученные модели (не в git)
│   ├── trained/            # .cbm файлы
│   └── exported/           # ONNX модели для MT5
├── src/                     # Исходный код Python
│   ├── data/               # Загрузка и обработка данных
│   ├── features/           # Feature engineering
│   ├── labeling/           # Стратегии разметки
│   ├── models/             # Обучение и валидация
│   ├── export/             # Экспорт в ONNX/MQL
│   └── backtesting/        # Быстрое тестирование
├── scripts/                 # Исполняемые скрипты
│   └── train_cluster_model.py
├── mql5/                    # MetaTrader 5 код
│   ├── Experts/            # Советники (.mq5)
│   └── Include/            # Include файлы (.mqh)
├── config/                  # Конфигурации
│   └── training_config.yaml
└── docs/                    # Документация
    └── implementation_plan.md
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Подготовка данных

Поместите CSV файлы в `data/raw/`:
```
XAUUSD_H1.csv  # Основной таймфрейм
XAU_1m_data.csv, XAU_5m_data.csv, ...  # Для мультифрейма
```

Формат CSV:
```
Date;Open;High;Low;Close;Volume
2004.06.11 00:00;389.50;390.00;388.75;389.25;1250
```

### 3. Обучение модели

```bash
python scripts/train_cluster_model.py
```

**Параметры поиска:**
- Целевая Val Accuracy: ≥75%
- Целевой R²: ≥0.95
- Максимум итераций: 40
- Автоматический выбор лучшей конфигурации

### 4. Экспорт в MT5

После успешного обучения:
- ONNX модели → `models/exported/`
- Include файлы → `models/exported/`
- Скопировать в MT5: `MQL5/Experts/` и `MQL5/Include/`

## ⚙️ Конфигурация

Редактировать `config/training_config.yaml`:

```yaml
symbol: XAUUSD_H1
direction: buy  # или sell

# Таргеты для поиска
target_val_acc: 0.75
target_r2: 0.95
max_iterations: 40

# Риск-менеджмент
stop_loss: 10.0
take_profit: 5.0

# Временные границы
backward: 2004-06-11
forward: 2020-01-01
full_forward: 2026-01-01
```

## 📊 Методология (6 этапов)

### Этап 1: Переформатирование данных
- Загрузка OHLCV данных
- Мультифреймовые признаки
- Целевая переменная с временным штрафом

### Этап 2: Динамические чекпоинты
- 10 блоков In-Sample
- 5 блоков Out-of-Sample (случайный порядок)
- Walk-forward валидация с реинициализацией

### Этап 3: Интеграция риск-менеджмента
- SL/TP на основе ATR
- Штраф за неисполненные сделки
- Фиксированный риск 0.5% на сделку

### Этап 4: Отбор модели
- Прохождение всех OOS-блоков
- Тест на Out-of-Time данных
- Критерий: положительная доходность, просадка ≤7-10%

### Этап 5: Live-валидация
- Демо-счет 6+ месяцев
- Мониторинг матожидания
- Контроль просадки

### Этап 6: Система деградации
- Автостоп при просадке >120% от исторической
- Триггер на 10 убыточных сделок подряд
- Требование перетренировки

## 🔧 Основные компоненты

### Кластеризация
```python
# Разбиение рынка на режимы
kmeans = KMeans(n_clusters=8)
data['clusters'] = kmeans.fit_predict(meta_features)
```

### Двухуровневая модель
```python
main_model = CatBoostClassifier()  # Торговые сигналы
meta_model = CatBoostClassifier()  # Фильтр кластеров
```

### Автопоиск
```python
SEARCH_SPACE = {
    'markup': [0.15, 0.18, 0.2, 0.22, 0.25, 0.28, 0.3, 0.35, 0.4],
    'n_clusters': [6, 7, 8, 9, 10, 11, 12],
    'model_depth': [4, 5, 6, 7],
    'model_iterations': [300, 500, 700, 1000]
}
```

## 📈 Метрики успеха

| Метрика | Целевое значение |
|---------|------------------|
| **Val Accuracy** | ≥75% |
| **R²** | ≥0.95 |
| **Просадка (OOS)** | ≤5% |
| **Просадка (OOT)** | ≤7-10% |
| **Баланс классов** | ≥0.2 |

## 🛡️ Риск-менеджмент

### В обучении:
- `Stop-Loss = Entry - 2.0 * ATR`
- `Take-Profit = Entry + 2.5 * ATR`
- Закрытие по таймауту: 20 баров

### В торговле (MT5):
- Фиксированный риск 0.5% от капитала
- Максимум позиций: 3
- Задержка между сделками: 5 часов
- Максимальный спред: 25 пунктов

## 🧪 Тестирование

Быстрый бэктест на истории:
```python
from src.backtesting import tester_one_direction

R2 = tester_one_direction(
    dataset,
    stop=10.0,
    take=5.0,
    forward=datetime(2020, 1, 1),
    backward=datetime(2004, 6, 11),
    markup=0.25,
    direction='buy',
    plt=True
)
```

## 📦 Зависимости

Основные пакеты:
- `catboost` - Градиентный бустинг
- `scikit-learn` - Кластеризация, метрики
- `pandas`, `numpy` - Обработка данных
- `numba` - Ускорение расчетов
- `scipy` - Фильтры (Savitzky-Golay)
- `matplotlib` - Визуализация

## 🔗 Интеграция с MT5

### Советник: `OneDirectionBot.mq5`
- Загрузка ONNX моделей через `OnnxCreateFromBuffer()`
- Расчет признаков на каждом баре
- Открытие/закрытие по сигналам
- Встроенный риск-менеджмент

### Include файл: `ModelInclude.mqh`
- Массивы периодов: `PeriodsXAUUSD_H1_0[]`
- Функции заполнения: `fill_arays()`, `fill_arays_m()`
- Ресурсы ONNX моделей

## ⚠️ Важные замечания

1. **Данные не включены в репозиторий** - загрузите из MetaTrader
2. **Модели не включены** - обучите локально
3. **Тестируйте на демо** - минимум 6 месяцев перед реальной торговлей
4. **Мониторьте деградацию** - система автоматически останавливается при ухудшении

## 📝 TODO для будущих агентов

- [ ] Реализовать мультифреймовую архитектуру (1m-1Month)
- [ ] Добавить систему Walk-Forward чекпоинтов (Этап 2)
- [ ] Интегрировать расчет ATR для динамических SL/TP
- [ ] Создать monitoring dashboard для live-торговли
- [ ] Реализовать систему автоматической деградации (Этап 6)
- [ ] Добавить юнит-тесты для критических компонентов

## 📄 Лицензия

Proprietary - For internal hedge fund use only

## 👤 Автор

Trading Systems Engineering Team

---

**⚡ Статус проекта:** Production-ready foundation  
**🎯 Текущая версия:** 1.0.0-alpha  
**📅 Последнее обновление:** 2025-01-02